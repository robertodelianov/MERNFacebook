import { makeStyles } from '@material-ui/core/styles';

export default makeStyles( () => ({
  Container: {
    marginBottom: '800px',
  },
  CircularProgress: {
    color: 'red',
    marginTop: '50px',
    marginBottom: '50px',
  }
}));
